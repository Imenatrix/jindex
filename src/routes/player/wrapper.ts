export function initVideoPlayer(target : Element) {
    return (window as any).initVideoPlayer(target)
}

export function setCamera(app : any, camera : string) {
    return (window as any).setCamera(app, camera)
}
